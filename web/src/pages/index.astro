---
import Layout from '../layouts/Layout.astro';
import ClimateMapApp from '../components/ClimateMapApp';
---

<Layout title="Mapa de Zonas Climáticas">
	<main class="container mx-auto px-4 py-12 md:py-20 min-h-screen flex flex-col items-center">
        <!-- Header Text -->
        <header class="text-center mb-16 animate-fade-in-up">
            <div class="inline-block mb-4 px-4 py-1.5 rounded-full border border-white/10 bg-white/5 backdrop-blur-md">
                <span class="text-sm font-medium text-indigo-300 tracking-wide uppercase">Versión 2.0.0</span>
            </div>
            <h1 class="text-5xl md:text-7xl font-bold text-white tracking-tight mb-6">
                Zonificación <span class="bg-clip-text text-transparent bg-gradient-to-r from-blue-400 to-purple-400">Inteligente</span>
            </h1>
            <p class="text-lg text-slate-400 max-w-2xl mx-auto leading-relaxed">
                Herramienta avanzada para la generación de mapas climáticos a partir de informes PDF y datos geoespaciales SHP.
            </p>
        </header>

        <!-- Main Application (Client Interaction) -->
        <div class="w-full">
		    <ClimateMapApp client:load />
        </div>
        
        <footer class="mt-20 text-center text-slate-500 text-sm">
            <p>&copy; {new Date().getFullYear()} Sistema de Análisis Geoespacial. Todos los derechos reservados.</p>
        </footer>
	</main>
</Layout>
